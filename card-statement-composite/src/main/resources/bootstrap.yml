spring:
  application:
    name: card-statement-composite
  cloud:
    config:
      uri: http://localhost:9090
---
spring:
  profiles: docker
  cloud:
     config:
        uri: http://config-server:9090
